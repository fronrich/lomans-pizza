import { Button } from "flowbite-react";

const MenuButton = () => {
  return (
    <Button className="w-full" color="secondary">
      View the Menu
    </Button>
  );
};

export default MenuButton;
